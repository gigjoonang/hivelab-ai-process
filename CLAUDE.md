# 이 저장소에서 일하는 방법

HIVELAB 디자인팀의 UI 구축 프로세스 저장소다. UI 프로젝트 관련 요청이 오면 임의로 진행하지 말고 이 프로세스를 따른다.

## 먼저 할 일

`WORKFLOW.md`를 읽는다. 그 표가 전체 흐름이고, 사용자가 지금 어디 있는지도 그 표로 판단한다.

## 기준은 어디 있나

기준은 문서 한 곳에만 둔다. 답변이나 스킬에 복사하지 말고 문서를 가리킨다.

| 무엇 | 어디 |
|---|---|
| 전체 흐름, 단계별 종료 기준 | `WORKFLOW.md` |
| AI 사용 원칙, A급/B급, 저작권·보안 | `docs/00-principles.md` |
| 단계별 상세 | `docs/01-stage-analysis.md`, `docs/02-stage-build.md`, `docs/03-stage-wrapup.md` |
| B급 시안 체크리스트 | `docs/quality-bar.md` |
| 레퍼런스 수집 규칙 | `docs/reference-rules.md` |
| 토큰·컴포넌트 명명 | `docs/design-system-rules.md` |
| 에셋 프롬프트와 규격 | `docs/asset-prompt-library.md` |

`docs/00-principles.md`랑 다른 얘기가 어디 적혀 있으면 `docs/00-principles.md`를 따른다.

## 스킬

단계별 스킬은 `.claude/skills/`에 있다. 단계 작업 요청이 오면 직접 처리하지 말고 해당 스킬을 부른다.

```
/ui-intake  /ui-analyze  /ui-structure  /ui-style
/ui-tokens  /ui-draft    /ui-assets
/ui-review  /ui-handoff     ← 10/31 작성 예정, 골격만 있음
```

## 지킬 것

1. **앞 단계 산출물 파일이 비어 있으면 시작하지 않는다.** 멈추고 사용자에게 알린다. 추측으로 채우지 않는다.
2. **1-4 방향 확정은 사람이 한다.** 구조와 스타일이 확정되기 전엔 `/ui-draft`로 넘어가지 않는다.
3. **`10/31 작성 예정` 표시가 있는 문서·템플릿·스킬의 빈칸을 채우지 않는다.** 채워진 것처럼 보이면 아무도 다시 안 본다.
4. **레퍼런스 URL은 전부 직접 열어본다.** 추측한 주소를 적지 않는다.
5. **대비 수치를 감으로 답하지 않는다.** 계산해서 실측값을 낸다.
6. **NDA가 있는 프로젝트에서는 고객사명으로 웹 검색을 하지 않는다.** `00-brief.md` 상단에서 확인한다.
7. **B급 시안은 고객사에 그대로 안 나간다.** 공유할 때마다 B급이라고 말한다.
8. **문서에 없는 걸 사실처럼 쓰지 않는다.** 추정은 추정이라고 표시한다.

## 작업 위치

- 프로젝트 작업물은 `projects/[프로젝트명]/` 안에만 만든다
- 새 프로젝트는 `./bin/new-project.sh [프로젝트명]`
- `projects/`에는 고객사 자료가 들어간다. 외부에 공개로 올리지 않는다

## Figma

생성과 수정은 `mcp__TalkToFigma__*` 툴로 한다. 연결이 없으면 사용자에게 플러그인 실행과 채널 이름을 요청한다.
Figma Dev Mode MCP 툴은 읽기 전용이다. 기존 시스템 참고할 때만 쓴다.

## 문서를 고칠 때

1. 어떤 프로젝트에서 무슨 문제가 있었는지 근거를 남긴다
2. 관련 스킬의 `SKILL.md`도 같이 본다. 기준을 스킬로 복사하지 말고 참조를 유지한다
3. `guide-site/index.html`은 문서를 사내용으로 옮긴 것이다. 문서를 크게 고쳤으면 여기도 갱신하고 다시 발행한다
